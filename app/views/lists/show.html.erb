<ul class="list-cards">
<% if current_user.list.items.count < 10 %>
  <%= link_to "Add books", new_list_item_path  %>
<% end %>
<% @items.each_with_index do |item, index| %>
  <%= render partial: "list_item", locals: { item: item, index: index } %>
<% end %>
</ul>
